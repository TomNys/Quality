<%= form_for(@hmac) do |f| %>
  <% if @hmac.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hmac.errors.count, "error") %> prohibited this hmac from being saved:</h2>

      <ul>
      <% @hmac.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br>
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :secret %>
  </div>
  <div class="field">
    <%= f.label :hashalgorithme %><br>
    <%= f.text_field :hashalgorithme %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
