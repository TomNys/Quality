<%= form_for(@dom0) do |f| %>
  <% if @dom0.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dom0.errors.count, "error") %> prohibited this dom0 from being saved:</h2>

      <ul>
      <% @dom0.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
